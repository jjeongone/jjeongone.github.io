services:
  chirpy:
    image: ruby:3.2
    working_dir: /srv
    volumes:
      - ./:/srv
    ports:
      - "4000:4000"
    command: >
      bash -lc "
        gem install bundler &&
        bundle install &&
        bundle exec jekyll serve --host 0.0.0.0 --port 4000
      "
